#include <stdio.h>

int main() {
    int a;
    scanf("%d", &a);

    if (a%2==0)
        printf("Positive number");
    else if (num < 0)
        printf("Negative number");
    else
        printf("Zero");

    return 0;
}