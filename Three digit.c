#include <stdio.h>

int main() {
    int n;
    scanf("%d", &num);

    if (n >= 100 && n <= 999)
        printf("Three-digit no");
    else
        printf("Not a three-digit no");

    return 0;
}
